# Environment variables and secrets
.env
.env.local
.env.*.local

# Google OAuth credentials
credentials.json
token.json

# Temporary files
.tmp/*
!.tmp/scripts/
!.tmp/scripts/**
.tmp/search_cache/*.json
.tmp/web_cache/*
!.tmp/search_cache/README.md
!.tmp/web_cache/README.md

# Memory database (regenerated, contains local state)
memory/*.db
memory/*.db-wal
memory/*.db-shm

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
env/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Knowledge graph — the scraped corpus in agent-data/knowledge-base/ IS
# committed (it's the stored knowledge); only the regenerable graph output
# and local source clones are ignored. See TODO.md for seeding.
graphify-out/
.tmp/klipper-src/

# Don't ignore .vscode - we want the settings
!.vscode/
